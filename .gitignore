# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/

# Test and coverage artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# Type checkers / linters
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Logs and local CI artifacts
*.log
coverage.json
# `make coverage-tools` writes its own report so the tools/ floors can be
# gated without diluting the package's number in coverage.json.
coverage-tools.json
spec-graph.json
spec-findings.json
head.json
base.json

# Witness store (CP-WM): ephemeral, per-checkout proof a stage actually ran.
# Never git-tracked -- freshness is enforced by comparing a witness's own
# recorded commit sha against the checkout's real HEAD, not by persisting
# history (DEC-WM-011).
.planlint/

# Docker / local builds
# (Dockerfile and .dockerignore are tracked; build context artifacts are not)

# Agent-skill evaluation runs (claude plugin eval writes here). Scrubbed
# summaries may be committed; raw transcripts never are -- see evals/README.md.
# `mocks/` is the runner's recorded MCP stand-ins, written beside the cases for
# the same reason and just as untracked.
evals/results/
evals/mocks/

# Hypothesis's example database. Local replay state for property-test
# failures; regenerated on demand and never a reviewable artifact.
.hypothesis/
